# Using k-nearest neighbors (KNN) algorithm for developing an algorithm to classify dwarf and non-dwarf galaxies based on only the basic observed properties, i.e. the brightness in two bands. 

# See the .ipynb file.
